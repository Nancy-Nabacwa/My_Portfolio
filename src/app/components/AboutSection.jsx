"use client";
import React, { useTransition, useState } from "react";
import Image from "next/image";
import TabButton from "./TabButton";

const TAB_DATA = [
  {
    title: "Skills",
    id: "skills",
    content: (
      <ul className="list-disc pl-2">
        <li>Next.js</li>
        <li>React</li>
        <li>JavaScript</li>
        <li>Python</li>
        <li>PostgreSQL</li>
        <li>Data Structures and Algorithms</li>
        <li>Jira</li>
      </ul>
    ),
  },
  {
    title: "Education",
    id: "education",
    content: (
      <ul className="list-disc pl-2">
        <li>Diploma in Information Technology</li>
        <li>AkiraChix-codeHive</li>
      </ul>
    ),
  },
];

const AboutSection = () => {
  const [tab, setTab] = useState("skills");
  const [isPending, startTransition] = useTransition();

  const handleTabChange = (id) => {
    startTransition(() => {
      setTab(id);
    });
  };

  return (
    <section className="text-white" id="about">
      <div className="md:grid md:grid-cols-2 gap-8 items-center py-8 px-4 xl:gap-16 sm:py-16 xl:px-16">
        <div className="mt-4 md:mt-0 text-left flex flex-col h-full">
          <h2 className="text-4xl font-bold text-white mb-4">About Me</h2>
          <p className="text-base lg:text-lg">
          I am passionate about the intersection of technology and innovation, 
          which has driven my journey in the tech field. With hands-on experience in data science, 
          I leverage data to make informed decisions, applying theoretical knowledge to solve real-world problems.
          Proficient in programming languages such as Python, and Javascript and frameworks like Django and React, 
          I thrive in fast-paced environments that require adaptability and strong problem-solving skills. 
          My role as a leader has enhanced my communication and teamwork abilities, allowing me to foster a collaborative learning environment with my peers.
          I envision contributing to society by utilizing my technical skills and passion for data-driven solutions.
          </p>
          <div className="flex flex-row justify-start mt-8">
            <TabButton
              selectTab={() => handleTabChange("skills")}
              active={tab === "skills"}
            >
              {" "}
              Skills{" "}
            </TabButton>
            <TabButton
              selectTab={() => handleTabChange("education")}
              active={tab === "education"}
            >
              {" "}
              Education{" "}
            </TabButton>
          </div>
          <div className="mt-8">
            {TAB_DATA.find((t) => t.id === tab).content}
          </div>
        </div>
      </div>
    </section>
  );
};

export default AboutSection;
